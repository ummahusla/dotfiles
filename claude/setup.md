# Claude Code setup

## Status line 

```
/statusline single line, fully color-coded with truecolor RGB gradient: repo name bold yellow, git branch in bold cyan with no parentheses,
20-block context bar using 24-bit RGB gradient from green(0,200,80) through yellow(220,200,0) to red(220,40,20) with dark gray(60,60,60) empty blocks, dynamic
emoji that changes with usage (🟢 under 20%, ⚡ 20-69%, 🔥 70-89%, 🚨 90%+), percentage colored by usage level, session cost in yellow, code velocity showing
+lines in green and -lines in red, model name + (context window) in parentheses in magenta, dim gray pipe separators between all elements
```
